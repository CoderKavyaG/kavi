import React from 'react'

const Resume = () => {
  return (
    <main className="pt-20 min-h-screen flex items-center justify-center" style={{ backgroundColor: 'var(--bg-color)', color: 'var(--text-color)' }}>
      <div className="max-w-2xl mx-auto px-6 py-16 text-center">
        <h1 className="text-3xl font-bold" style={{ color: 'var(--text-color)' }}>Resume</h1>
        <p className="mt-4 text-sm" style={{ color: 'var(--text-secondary)' }}>Resume — currently being built</p>
      </div>
    </main>
  )
}

export default Resume
